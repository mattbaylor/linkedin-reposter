# Infisical Configuration
# Get your Service Token from: https://infisical.example.com/
# Navigate to: LinkedInReposter → Settings → Service Tokens → Create Token
# The token format will be: st.xxxxx.yyyyy.zzzzz

INFISICAL_URL=https://infisical.example.com
INFISICAL_TOKEN=st.your-service-token-here
INFISICAL_PROJECT_ID=LinkedInReposter
INFISICAL_ENVIRONMENT=dev

# Machine Identity for cross-project access (optional, for Copilot token sync)
OPENCODE_INFISICAL_PROJECT_ID=your-opencode-project-id
INFISICAL_MACHINE_IDENTITY_CLIENT_ID=your-machine-identity-client-id
INFISICAL_MACHINE_IDENTITY_CLIENT_SECRET=your-machine-identity-client-secret

# VNC Password (REQUIRED for security - set a strong password)
VNC_PASSWORD=your-strong-vnc-password-here

# Application Configuration (optional - has defaults)
APP_PORT=8080
AI_MODEL=gpt-4o

# Note: All other secrets (LinkedIn, Postal, GitHub Token, etc.) 
# are stored in Infisical and will be fetched automatically at runtime
